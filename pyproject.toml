[project]
name = "ai-software-scanner"
version = "0.1.1"
description = "Analyze software projects using AI to identify potential issues and improvements."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "openai>=2.9.0",
    "openpyxl>=3.1.5",
]
